(ns cross-universe-communicator.core-test
  (:require [clojure.test :refer :all]
            [cross-universe-communicator.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
